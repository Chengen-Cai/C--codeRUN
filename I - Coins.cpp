#include<bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin>>n;
    vector<vector<int>> nums;
    vector<int> numinnums(2);
    int num[n];
    for (size_t i = 0; i < n; i++)
    {
        cin>>num[i];
    }
    int head=0,tail=0;
    
    for (size_t i = 0; i < n; i++)
    {
        vector<int> snum(2);
        //num[i];


        //1-num[i];


    }


        
    }








    system("PAUSE");



}